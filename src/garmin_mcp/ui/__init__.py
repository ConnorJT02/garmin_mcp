"""
MCP Apps UI layer for Garmin MCP Server

Registers ui:// resources (interactive charts) and exposes the metadata
needed to link them to specific tools via _meta.ui.resourceUri, per the
MCP Apps extension spec (SEP-1865).
"""
